export const endpoints = {
  portfolio: '/api/portfolio',
  leads: '/api/leads',
  callback: '/api/callback',
  cabinet: {
    status: '/api/cabinet/status',
    comments: '/api/cabinet/comments',
    uploads: '/api/cabinet/uploads',
  }
}
